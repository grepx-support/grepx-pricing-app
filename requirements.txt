# Root Requirements - Shared Dependencies
# Each service has its own requirements.txt with pinned versions
# This file documents shared core packages that MUST be consistent across services

# Message Broker & Caching
celery==5.4.0
redis==5.0.1
flower==2.0.1

# Database & ORM
pymongo>=4.0.0

# Data Processing
pandas>=2.0.0
numpy>=1.26.0
yfinance>=0.2.0

# Configuration
pyyaml==6.0.2
hydra-core==1.3.2
omegaconf>=2.3.0

# Utilities
python-dotenv
requests>=2.28.0

# NOTE: Service-specific packages managed in:
# - servers/grepx-celery-server/requirements.txt
# - servers/grepx-dagster-server/requirements.txt
# - servers/grepx-apache-airflow-server/requirements.txt
# - servers/grepx-task-generator-server/requirements.txt
# - shared/grepx-shared-models/requirements.txt